const superHeroes: string[] = []
const heroPower: Array<number> = []

type User = {
    name: string
    isActive: boolean
}

const allUsers: User[] = []

const MLModels: number[][] = [
    [255, 255, 255]
]

superHeroes.push("femi")
heroPower.push(2)
allUsers.push({name: "femi", isActive: false})

